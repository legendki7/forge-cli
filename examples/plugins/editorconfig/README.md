# EditorConfig plugin example

Validate this entirely declarative example with:

```sh
forge plugins validate examples/plugins/editorconfig
```

It declares only file generation, Stack Builder metadata, and bounded scanner evidence. It contains no
code and cannot execute commands.
