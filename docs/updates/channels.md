# Update Channels

ForgeKi supports Stable and Beta. Beta remains the development default. Channel selection chooses
only a preconfigured trusted metadata provider; users cannot enter arbitrary URLs. Moving between
channels never bypasses version, signature, expiry, digest, or downgrade policy.
