# Marketplace Privacy

ForgeKi requests only fixed signed metadata and the selected package. Search runs locally against the
verified index. It does not send project/workspace names, paths, dependencies, source code,
environment values, secrets, user/GitHub/npm identity, install events, or a persistent client ID.
No telemetry, tracking pixels, publisher avatars, remote plugin images, or analytics were added.
