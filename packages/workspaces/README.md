# `@forgecli7/workspaces`

Shared ForgeKi engine for strictly typed multi-service workspace definitions, compatibility,
deterministic ports and environments, generation plans, atomic execution, and read-only scanning.

The package accepts only known service and connection identifiers. It does not install dependencies,
run generated commands, start Docker, connect to databases, or access the network.
