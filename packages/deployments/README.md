# `@forgecli7/deployments`

Shared, framework-independent environment and deployment configuration planning for ForgeKi.

The package validates environment schemas, assesses target compatibility and readiness, produces deterministic reviewable files, exports without overwriting, and reports drift. It never deploys, contacts a cluster, reads secret values, or executes generated configuration.
